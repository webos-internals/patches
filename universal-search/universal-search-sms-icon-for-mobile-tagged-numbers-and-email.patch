diff --git a/usr/lib/luna/system/luna-applauncher/app/controllers/global-search-assistant.js b/usr/lib/luna/system/luna-applauncher/app/controllers/global-search-assistant.js
index 6f3368e..92fd76a 100644
--- a/usr/lib/luna/system/luna-applauncher/app/controllers/global-search-assistant.js
+++ b/usr/lib/luna/system/luna-applauncher/app/controllers/global-search-assistant.js
@@ -611,13 +611,16 @@ GlobalSearchAssistant = Class.create({
 				a.contactId = contact.id;
 				a.label = that.mailLabel[a.label];
 				mailList.push(a);
+				a.icon = "im-image";
 			});
 		}
 		if (data.record.phoneNumbers && data.record.phoneNumbers.length > 0) {
 			$A(data.record.phoneNumbers).each(function(a){
 				a.contactId = contact.id;
 				a.value = Mojo.Format.formatPhoneNumber(a.value);
-				a.icon = "im-image";
+				//a.icon = "im-image";
+				if (a.label == 3)
+					a.icon = "im-image";
 				if (a.label == 2 && a.customLabel) 
 					a.label = a.customLabel;
 				else 
