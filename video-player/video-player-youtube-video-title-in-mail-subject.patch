diff --git a/usr/palm/frameworks/metascene.videos.share/submission/67/concatenated.js b/usr/palm/frameworks/metascene.videos.share/submission/67/concatenated.js
index 0f261f0..aefe809 100755
--- a/usr/palm/frameworks/metascene.videos.share/submission/67/concatenated.js
+++ b/usr/palm/frameworks/metascene.videos.share/submission/67/concatenated.js
@@ -72,7 +72,7 @@ ShareService.Facebook = {
 };
 
 ShareService.Email = {
-	link : function(sceneController, videoUrl){
+	link : function(sceneController, videoUrl, videoTitle){
 		sceneController.serviceRequest(
 			'palm://com.palm.applicationManager', {
 			method: 'open',
@@ -80,6 +80,7 @@ ShareService.Email = {
 				id: 'com.palm.app.email',
 				params: {
 				recipients: [],
+				summary : videoTitle,
 				text : videoUrl
 			}}
 		});
@@ -681,7 +682,7 @@ UploadshareAssistant.prototype = {
 	},
 	
 	handleEmailIcon : function(){
-		ShareService.Email.link(this.controller, this.link + " - " + this.video.title);
+		ShareService.Email.link(this.controller, this.link + " - " + this.video.title, this.video.title);
 		this.closeDashboard();
 	},
 	
@@ -1568,7 +1569,7 @@ var SharemenuAssistant = function(){
 			case "link":
 				var startIndex = 11; // "email_link_".length === 11		
 				ShareService.Email.link(sceneController, 
-					cmd.substr(startIndex) + " - " + vid.details.title);
+					cmd.substr(startIndex) + " - " + vid.details.title, vid.details.title);
 			break;
 		}
 	}
