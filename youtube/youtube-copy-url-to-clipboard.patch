diff --git a/usr/palm/applications/com.palm.app.youtube/app/controllers/video-assistant.js b/usr/palm/applications/com.palm.app.youtube/app/controllers/video-assistant.js
index 5c3be25..578254d 100644
--- a/usr/palm/applications/com.palm.app.youtube/app/controllers/video-assistant.js
+++ b/usr/palm/applications/com.palm.app.youtube/app/controllers/video-assistant.js
@@ -78,6 +78,10 @@ var VideoAssistant = Class.create({
 						}});
 				event.stop(event);
 			}	
+			else if (event.command == 'clipboard' && this.video.shareLink && this.video.shareLink != "") {
+				this.controller.stageController.setClipboard(this.video.shareLink);
+				event.stop(event);
+			}
 			else {
 				AppAssistant.common.handleCommand(event);	
 			}
@@ -93,7 +97,8 @@ var VideoAssistant = Class.create({
 	shareMenuModel: {label: $L('Share'), 
 					items: [
 						{label: $L('Email'), command:'email'}, 
-						{label: $L('Text Message'), command:'msg' }
+						{label: $L('Text Message'), command:'msg'},
+						{label: $L('Copy URL to Clipboard'), command:'clipboard'}
 					]}
 	
 });
