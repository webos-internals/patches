diff --git a/usr/palm/applications/com.palm.app.contacts/app/controllers/list-assistant.js b/usr/palm/applications/com.palm.app.contacts/app/controllers/list-assistant.js
index e8f642a..9dd7fa2 100644
--- a/usr/palm/applications/com.palm.app.contacts/app/controllers/list-assistant.js
+++ b/usr/palm/applications/com.palm.app.contacts/app/controllers/list-assistant.js
@@ -109,6 +109,11 @@ ListAssistant = Foundations.Class.create(App.Scene, {
 			items: [Mojo.Menu.editItem, {
 				label: $L("Send All to Car Kit"),
 				command: "sendcontacts"
+//***** ----------------------------------------Patch Email contacts 1/5
+				}, {
+				label:$L("Send All via Email"),
+				command:"emailcontacts"
+//***** ----------------------------------------end patch
 			}, prefsItem, {
 				label: $L('Help'),
 				command: Mojo.Menu.helpCmd
@@ -350,6 +355,10 @@ ListAssistant = Foundations.Class.create(App.Scene, {
 				this.controller.stageController.pushScene('prefs');
 			} else if (event.command === "sendcontacts") {
 				this.sendContacts();
+//***** ----------------------------------------Patch Email contacts 2/5
+			} else if (event.command === "emailcontacts") {
+				this.emailContacts();
+//***** ----------------------------------------end patch
 			}
 		}
 		
@@ -372,7 +381,9 @@ ListAssistant = Foundations.Class.create(App.Scene, {
 			this.vcardRequest = AppAssistant.contactsService.makeVCardList(this.controller, {
 				onlyPhoneNumbers: true,
 				filePath: AppAssistant.contactsService.getCarkitFileName()
-			}, this.gotVCard.bind(this));
+//***** ----------------------------------------Patch Email contacts 3/5
+			}, this.gotVCard.bind(this, "bluetooth"));
+//***** ----------------------------------------end patch
 			var cancelFn = function () {
 				this.vcardRequest.cancel();
 				this.vcardRequest = null;
@@ -387,7 +398,9 @@ ListAssistant = Foundations.Class.create(App.Scene, {
 		}
 	},
 	
-	gotVCard: function (resp) {
+//***** ----------------------------------------Patch Email contacts 4/5
+gotVCard:function(type, resp){
+	if (type == "bluetooth") {
 		var appArgs = {
 				appId: "com.palm.app.bluetooth",
 				name: "btopp"
@@ -398,6 +411,45 @@ ListAssistant = Foundations.Class.create(App.Scene, {
 		
 		this.sendContactsPopupAssistant.widget.mojo.close();
 		this.controller.stageController.pushScene(appArgs, sceneArgs);
+	}
+	else if (type == "email") {
+		var vcfPath = resp.file;
+		
+		this.sendContactsPopupAssistant.widget.mojo.close();
+		this.controller.serviceRequest('palm://com.palm.applicationManager', {
+			method: 'open',
+			parameters: {
+				id: 'com.palm.app.email',
+				params: {
+					summary: 'Pre Contacts in vCard format',
+					recipients: [ 'you@self.com' ],
+					text: 'Attachment contains all contacts from the Pre in vCard format. Linked contacts show up under one contact: the main one as on the Pre.',
+					attachments: [ {fullPath:resp.filePath, mimeType:'text/txt'} ]		//				mimeType:'text/x-vcard'
+				}
+			}
+		});	
+	}
+},
+
+	emailContacts:function(){
+			// this is largely a copy of bluetoothResponse: function
+			this.vcardRequest = AppAssistant.contactsService.makeVCardList(this.controller, {
+				onlyPhoneNumbers: false,
+//				stripPhoneNumbers:false
+				filePath: AppAssistant.contactsService.getCarkitFileName()
+			}, this.gotVCard.bind(this, "email"));
+			var cancelFn = function () {
+				this.vcardRequest.cancel();
+				this.vcardRequest = null;
+			}.bind(this);
+			Mojo.Log.info("Setting up a dialog");
+			this.sendContactsPopupAssistant = new SendContactsPopupAssistant(this.controller, cancelFn);
+			this.controller.showDialog({
+				template: 'list/send-contact-popup',
+				assistant: this.sendContactsPopupAssistant,
+				preventCancel: true
+			});
+//***** ----------------------------------------end patch
 	},
 	
 	handleListTap: function (event) {
diff --git a/usr/palm/applications/com.palm.app.contacts/app/views/list/send-contact-popup.html b/usr/palm/applications/com.palm.app.contacts/app/views/list/send-contact-popup.html
index f70ea5b..c85a34c 100644
--- a/usr/palm/applications/com.palm.app.contacts/app/views/list/send-contact-popup.html
+++ b/usr/palm/applications/com.palm.app.contacts/app/views/list/send-contact-popup.html
@@ -1,6 +1,6 @@
 <div class="palm-dialog-content">   
     <div class="palm-dialog-title" x-mojo-loc=''>
-		Send All To Car Kit
+		Sending vCards (Bluetooth/eMail as chosen)
 	</div>
 	<div class="palm-dialog-separator"></div>
 	<div class="palm-dialog-message">
