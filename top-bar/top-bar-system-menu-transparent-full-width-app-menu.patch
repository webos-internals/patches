--- .orig/usr/lib/luna/system/luna-systemui/app/controllers/bar-assistant.js
+++ /usr/lib/luna/system/luna-systemui/app/controllers/bar-assistant.js
@@ -283,6 +283,7 @@
 		this.foregroundAppID = payload.id;
 		$('appmenu').hide();
 		$('carrier').show();
+		$('devicemenu').className = "";
 		return;
 	}
 	
@@ -313,11 +314,13 @@
 		else 
 			$('appname').innerHTML = payload.appmenu || payload.title;
 		$('appmenu').show();
+		$('devicemenu').className = "devicemenu-appmenu";
 		this.foregroundAppID = payload.id;
 	}
 	else{
 		$('appmenu').hide();
 		$('carrier').show();
+		$('devicemenu').className = "";
 		this.foregroundAppID=null;
 	}
 },
@@ -405,10 +408,12 @@
 		if (payload.locked || this.foregroundAppID == null) {
 			$('carrier').show();
 			$('appmenu').hide();
+			$('devicemenu').className = "";
 		}
 		else {
 			$('carrier').hide();
 			$('appmenu').show();
+			$('devicemenu').className = "devicemenu-appmenu";
 		}
 	}	
 	
--- .orig/usr/palm/frameworks/mojo/submissions/338/stylesheets/global-menus.css
+++ /usr/palm/frameworks/mojo/submissions/338/stylesheets/global-menus.css
@@ -659,8 +659,8 @@
 #palm-app-menu.palm-popup-container {
   top: -2px;
   left: -6px;
-  min-width: 306px;
-  max-width: 306px;
+  min-width: 332px;
+  max-width: 332px;
   max-height: 413px;
 }
 
--- .orig/usr/lib/luna/system/luna-systemui/stylesheets/systemui.css
+++ /usr/lib/luna/system/luna-systemui/stylesheets/systemui.css
@@ -36,8 +36,9 @@
 		top: 0;
 		left: 0;
 		height: 28px;
-		width: 140px;
+		width: 100%;
 		padding: 0;
+		background: transparent;
 }
 
 #date-and-time {
@@ -68,6 +69,7 @@
 		max-height: 26px;
 		float: left;
 		border-width: 0 20px 0 13px;
+		min-width: 287px;
 		-webkit-border-image: url(../images/appname-background.png) 0 20 0 13 repeat repeat;
 }
 
@@ -394,6 +396,15 @@
 		background-color: black;
 }
 
+.devicemenu-appmenu {
+		position: fixed;
+		margin: 0;
+		top: 0;
+		right: 0;
+		padding-right: 8px;
+		width: 120px;
+}
+
 #statusmenu {
 		color: #fff;
 		position: fixed;
