diff --git a/usr/lib/luna/system/luna-systemui/app/controllers/bar-assistant.js b/usr/lib/luna/system/luna-systemui/app/controllers/bar-assistant.js
index 6cb3941..9406105 100644
--- a/usr/lib/luna/system/luna-systemui/app/controllers/bar-assistant.js
+++ b/usr/lib/luna/system/luna-systemui/app/controllers/bar-assistant.js
@@ -267,7 +267,7 @@ updateAppMenuTitle: function(payload) {
 	if (payload.title) {
 		$('carrier').hide();
 		if (payload.id == "com.palm.app.phone" || payload.id == "com.palm.app.firstuse"){ 
-			$('appname').innerHTML = this.carrierText;
+			$('appname').innerHTML = payload.appmenu || payload.title;
 			this.phoneIsForeground = true;
 		}
 		else if (this.isPreferences(payload))  {
@@ -359,8 +359,8 @@ handleStatusChange: function(payload) {
 		
 	if (payload.locked != null) {
 		if (payload.locked) {
+			$('date').hide();
 			$('clock').hide();
-			$('date').show();
 			this.deviceLocked = true;
 		}
 		else {
@@ -547,7 +547,7 @@ handlePowerQueryNotifications: function(payload){
 				$('rssi').className = "flight-mode";
 			}
 			else {
-				this.carrierText = $L("Phone offline");
+				this.carrierText = new Date().toDateString();
 				$('carrier').innerHTML = this.carrierText;
 			}							
 			$('roaming').hide();
@@ -573,7 +573,7 @@ handleNetworkStatusQueryNotification: function(payload) {
 			if(this.demoBuild)
 				this.carrierText = "Palm";
 			else	
-				this.carrierText = $L(payload.extended.networkName.escapeHTML());
+				this.carrierText = new Date().toDateString();
 			$('carrier').innerHTML = this.carrierText;	   	
 			if(payload.extended.registration == 'home') {			
 				$('roaming').hide();
@@ -916,7 +916,7 @@ handletelephonyNotifications: function(payload) {
 				this.updateAirplaneModeProgress('phone');
 			}
 			else {
-				this.carrierText = $L("Phone offline");
+				this.carrierText = new Date().toDateString();
 				$('carrier').innerHTML = this.carrierText;
 			}
 			$('datanet').className = 'off';
@@ -939,7 +939,7 @@ handletelephonyNotifications: function(payload) {
 				if(this.demoBuild)
 					this.carrierText = "Palm";
 				else	
-					this.carrierText = $L(networkMsg.networkName.escapeHTML());
+					this.carrierText = new Date().toDateString();
 				$('carrier').innerHTML = this.carrierText;
 				this.simlocked = false;
 				
@@ -2628,6 +2628,9 @@ updateClockEveryMinute: function() {
 	var date = new Date();
 	var format = Mojo.Format.getFormatHash()['shortTime24'];
 	
+	this.carrierText = new Date().toDateString();
+	$('carrier').innerHTML = this.carrierText;
+	
 	if (Mojo.Format.using12HrTime()) {
 		if (date.getHours() > 12) {
 			date.setHours(date.getHours() % 12);
