diff --git a/usr/lib/luna/system/luna-systemui/app/controllers/bar-assistant.js b/usr/lib/luna/system/luna-systemui/app/controllers/bar-assistant.js
index 5dc60f6..e7615f6 100644
--- a/usr/lib/luna/system/luna-systemui/app/controllers/bar-assistant.js
+++ b/usr/lib/luna/system/luna-systemui/app/controllers/bar-assistant.js
@@ -296,7 +296,7 @@ updateAppMenuTitle: function(payload) {
 	if (payload.title) {
 		$('carrier').hide();
 		if (payload.id == "com.palm.app.phone" || payload.id == "com.palm.app.firstuse"){ 
-			$('appname').innerHTML = this.carrierText;
+			$('appname').innerHTML = payload.appmenu || payload.title;
 			this.phoneIsForeground = true;
 		}
 		else if (this.isPreferences(payload))  {
@@ -388,8 +388,8 @@ handleStatusChange: function(payload) {
 		
 	if (payload.locked != null) {
 		if (payload.locked) {
-			$('clock').hide();
-			$('date').show();
+			$('clock').show();
+			$('date').hide();
 			this.deviceLocked = true;
 		}
 		else {
@@ -582,8 +582,8 @@ handlePowerQueryNotifications: function(payload){
 				$('rssi').className = "flight-mode";
 			}
 			else {
-				this.carrierText = this.$L("Phone offline");
-				this.carrierTextEnglish = "Phone offline";
+				this.carrierText = new Date().toDateString();
+				this.carrierTextEnglish = this.carrierText;
 				$('carrier').innerHTML = this.carrierText;
 			}							
 			$('roaming').hide();
@@ -611,8 +611,8 @@ handleNetworkStatusQueryNotification: function(payload) {
 				this.carrierTextEnglish = "Palm";
 			}
 			else{
-				this.carrierText = this.$L(payload.extended.networkName.escapeHTML());
-				this.carrierTextEnglish = payload.extended.networkName.escapeHTML();
+				this.carrierText = new Date().toDateString();
+				this.carrierTextEnglish = this.carrierText;
 			} 
 				
 			$('carrier').innerHTML = this.carrierText;	   	
@@ -988,8 +988,8 @@ handletelephonyNotifications: function(payload) {
 				this.updateAirplaneModeProgress('phone');
 			}
 			else {
-				this.carrierText = this.$L("Phone offline");
-				this.carrierTextEnglish = "Phone offline";
+				this.carrierText = new Date().toDateString();
+				this.carrierTextEnglish = this.carrierText;
 				$('carrier').innerHTML = this.carrierText;
 			}
 			$('datanet').className = 'off';
@@ -1015,8 +1015,8 @@ handletelephonyNotifications: function(payload) {
 				}
 					
 				else{
-					this.carrierText = this.$L(networkMsg.networkName.escapeHTML());
-					this.carrierTextEnglish = networkMsg.networkName.escapeHTML();
+					this.carrierText = new Date().toDateString();
+					this.carrierTextEnglish = this.carrierText;
 				}	
 					
 				$('carrier').innerHTML = this.carrierText;
@@ -2761,6 +2761,10 @@ updateClockEveryMinute: function() {
 	format = format.replace(/\s?a\s?/,"");	
 	$('clock').innerHTML = Mojo.Format.formatDate(date, {time: format});				
 	$('today').innerHTML = Mojo.Format.formatDate(date,{date:'short'});
+
+	this.carrierText = new Date().toDateString();
+	$('carrier').innerHTML = this.carrierText;
+
 },
 
 /*
