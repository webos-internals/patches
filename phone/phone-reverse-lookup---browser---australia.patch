diff --git a/usr/palm/applications/com.palm.app.phone/app/controllers/calllog-assistant.js b/usr/palm/applications/com.palm.app.phone/app/controllers/calllog-assistant.js
index 2cb234a..5bb1ce3 100644
--- a/usr/palm/applications/com.palm.app.phone/app/controllers/calllog-assistant.js
+++ b/usr/palm/applications/com.palm.app.phone/app/controllers/calllog-assistant.js
@@ -480,15 +480,17 @@ var CalllogAssistant = Class.create({
 			};
 			view_contact = Mojo.View.render({template: 'calllog/listitem_contact', object: template_model});
 		}
-		
+
 		var element = Mojo.View.convertToNode('<div name="rowdrawer" class="calllogdetails" x-mojo-element="Drawer"> '
-											  + call_details + '<div name="contact_numbers_placeholder"></div>' + view_contact
+											  + call_details + '<div name="contact_numbers_placeholder"></div>'
+											  + '<div class="palm-row" x-mojo-touch-feedback="momentary" name="dolookup" value="' + itemModel.contactId + '" number="' + itemModel.number + '"><div class="palm-row-wrapper"><div class="title truncating-text">Reverse Lookup (AU)</div></div></div>'
+											  + view_contact
 									  		  + '</div>', this.controller.window.document);
 		
 		itemNode.querySelector(".palm-row-wrapper").appendChild(element);
 		
 		this.loadContactNumbers(element, itemModel, is_valid);
-		
+
 		// no 'add to contact' or altnum row
 		if ( ! is_valid && itemModel.contactId == -1 ) {
 			element.querySelector('div[name="call-log-details-row"]').removeClassName('first').addClassName('single');
@@ -620,6 +622,25 @@ var CalllogAssistant = Class.create({
 						this.dialNumber(details.number, itemModel.contactId, details.label);
 					}
 					return;
+
+				case "dolookup":
+					// Launch the Browser with Reverse Australia lookup
+					TheLookupURL = "http://www.reverseaustralia.com/lookup/" + target.getAttribute('number') + "/"; 
+					TheLookupURL =  TheLookupURL.replace("+","00");
+
+			
+					this.controller.serviceRequest("palm://com.palm.applicationManager", {
+					   method: "open",
+					   parameters:  {
+					       id: 'com.palm.app.browser',
+					       params: {
+						   target: TheLookupURL
+					       }
+					   }
+					 }); 
+					
+					return;
+				
 					
 				case "contact":
 					if ( target.getAttribute('value') == -1 ) {
