diff --git a/usr/palm/applications/com.palm.app.browser/index.html b/usr/palm/applications/com.palm.app.browser/index.html
index da4e53d..410e1b8 100644
--- a/usr/palm/applications/com.palm.app.browser/index.html
+++ b/usr/palm/applications/com.palm.app.browser/index.html
@@ -6,8 +6,50 @@
 	<!--<script src="/media/work/git/enyo-dev/framework/enyo.js" type="text/javascript" launch="debug"></script>//-->
 </head>
 <body>
-<script type="text/javascript">
-	enyo.create({kind: "BrowserApp"}).renderInto(document.body);
-</script>
+    <script type="text/javascript">
+        var topComponent = enyo.create({
+            kind: "Pane",
+            style: "background: #313131 url(images/background-startpage.png) center center no-repeat; -webkit-background-size: 100% 100%; color: #eee",
+            components: [
+                {name: "redirectView", kind: "VFlexBox", align: "center", pack: "center", components: [
+                    {kind: "SpinnerLarge", showing: true},
+                    {content: "Redirecting to Isis Web Browser..."}
+                ]},
+                {name: "abMissingView", kind: "VFlexBox", align: "center", pack: "center", components: [
+                    {content: "You don't have Isis Web!", style: "padding: 20px"},
+                    {kind: "VFlexBox", style: "width: 300px", components: [
+                        {kind: "Button", layoutKind: "HFlexLayout", align: "center", pack: "center", style: "height: 80px; font-size: 20pt", caption: "Get It Now!", onclick: "openAppCat"},
+                        {kind: "Button", className: "enyo-button-dark", caption: "Load Stock Browser", onclick: "openStockBrowser"}
+                    ]}
+                ]},
+                {kind: "Dashboard", name: "dashboard", onTap: "dashboardTapped"},
+                {name: "launchApp", kind: "PalmService", service: "palm://com.palm.applicationManager/", method: "open", onFailure: "serviceFailure", onSuccess: "serviceSuccess"},
+                {kind: "ApplicationEvents", onApplicationRelaunch: "relaunchHandler"}
+            ],
+            serviceSuccess: function() {
+                window.close();
+            },
+            serviceFailure: function() {
+                this.selectViewByName("abMissingView");
+            },
+            openAppCat: function() {
+                this.$.launchApp.call({id: 'com.palm.app.enyo-findapps', params: {target: "http://www.webosnation.com"}});
+            },
+            openStockBrowser: function() {
+	            enyo.create({kind: "BrowserApp"}).renderInto(document.body);
+	            this.destroy();
+            },
+            relaunchHandler: function() {
+                this.redirect(enyo.windowParams);
+            },
+            redirect: function(params) {
+                this.selectViewByName("redirectView");
+                this.$.launchApp.call({id: 'com.junoavalon.app.isisbrowser', params: params});
+            }
+        }).renderInto(document.body);
+        var paramsString = window.PalmSystem && PalmSystem.launchParams || "{}";
+        var params = JSON.parse(paramsString);
+        topComponent.redirect(params);
+    </script>
 </body>
 </html>
