diff --git a/usr/palm/applications/com.palm.app.clock/app/controllers/app-assistant.js b/usr/palm/applications/com.palm.app.clock/app/controllers/app-assistant.js
index 36e0711..9d556a1 100644
--- a/usr/palm/applications/com.palm.app.clock/app/controllers/app-assistant.js
+++ b/usr/palm/applications/com.palm.app.clock/app/controllers/app-assistant.js
@@ -19,6 +19,7 @@ function AppAssistant(appController) {
 	
 	
 	this.clocClock = false;
+	this.exhibitClock = false;
 	
 	this.changeTheme = this.changeTheme.bind(this);
 	
@@ -45,6 +46,7 @@ AppAssistant.prototype.setClockFocused = function(focused) {
 AppAssistant.prototype.handleLaunch = function(params) {
 	
 	if(params){
+		this.dockMode = params.dockMode
 		switch (params.action) {
 			case "ring":
 				this.ring(params);
@@ -151,7 +153,7 @@ AppAssistant.prototype.registerForTimeChanges  = function() {
 AppAssistant.prototype.launchClock = function(sceneParams) {
 	var clockStage = this.controller.getStageController('clock');
 
-	if (clockStage) {
+	if (clockStage && !this.dockMode) {
 		clockStage.window.focus(); 
 		if (sceneParams) {
 			clockStage.delegateToSceneAssistant("showScene", sceneParams);
@@ -165,7 +167,11 @@ AppAssistant.prototype.launchClock = function(sceneParams) {
 		var f = function(stageController) {
 			stageController.pushScene('clock', sceneParams);
 		}.bind(this);
-		this.controller.createStageWithCallback({name: 'clock', lightweight: true}, f);		
+		if(this.dockMode && !this.controller.getStageController('exhibition-clock')) {
+			this.controller.createStageWithCallback({name: 'exhibition-clock', lightweight: true}, f, "dockMode");	
+		} else {
+			this.controller.createStageWithCallback({name: 'clock', lightweight: true}, f);
+		}
 	}
 };
 
@@ -176,6 +182,9 @@ AppAssistant.prototype.changeTheme = function(themeName) {
 		//console.log("clocK Clock themeName changed " + themeName);
 		this.clocClock.changeThemeProxy(themeName);
 	}
+	if(this.exhibitClock){
+		this.exhibitClock.changeThemeProxy(themeName);
+	}
 };
 
 AppAssistant.prototype.launchAlarmList = function() {
diff --git a/usr/palm/applications/com.palm.app.clock/app/controllers/clock-assistant.js b/usr/palm/applications/com.palm.app.clock/app/controllers/clock-assistant.js
index 3e75794..2575abd 100644
--- a/usr/palm/applications/com.palm.app.clock/app/controllers/clock-assistant.js
+++ b/usr/palm/applications/com.palm.app.clock/app/controllers/clock-assistant.js
@@ -68,7 +68,12 @@ var ClockAssistant = Class.create({
 		this.controller.document.body.addClassName(Mojo.Locale.getCurrentLocale());
 		
 		//add ourself to the app assistant
-		this.appAssistant.clocClock = this;
+		if(this.controller.stageController.window.name=="exhibition-clock") {
+			this.appAssistant.exhibitClock = this;
+		} else {
+			this.appAssistant.clocClock = this;
+		}
+		
 		
 		this.updateClockHelper = this.updateClockHelper.bind(this);
 	},
@@ -156,6 +161,9 @@ var ClockAssistant = Class.create({
 	},
 	
 	showMenu: function(event) {
+		if(this.controller.stageController.window.name=="exhibition-clock") {
+			return;
+		}
 		if(this.timeOutCounter){
 			this.hideMenu();
 		}else{
@@ -183,7 +191,11 @@ var ClockAssistant = Class.create({
 	
 	aboutToActivate: function(){
 		if(this.firstActivation){
-			this.showMenu();
+			if(this.controller.stageController.window.name!="exhibition-clock") {
+				this.showMenu();
+			} else {
+				this.hideMenu();
+			}
 			this.firstActivation = false;
 		}else{
 			
